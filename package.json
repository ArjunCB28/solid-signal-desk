{
  "name": "signal-desk",
  "type": "module",
  "scripts": {
    "dev": "vinxi dev",
    "build": "vinxi build",
    "start": "vinxi start",
    "preview": "vinxi build && npx wrangler pages dev dist",
    "deploy": "vinxi build && npx wrangler pages deploy dist",
    "cf-typegen": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.12.4",
    "vitest": "^4.1.7",
    "wrangler": "^4.94.0"
  },
  "dependencies": {
    "@solidjs/router": "^0.15.0",
    "@solidjs/start": "^1.3.2",
    "solid-js": "^1.9.5",
    "vinxi": "^0.5.11"
  },
  "engines": {
    "node": ">=22"
  }
}
